import React from "react";
import { withRouter } from "react-router-dom";

const Painel = () => {

    return (
        <>
            <label>Esse é o painel</label>
        </>
    );
}

export default withRouter(Painel);